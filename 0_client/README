一、关于交易有关术语的定义:

欲交易某合约:
1.交易网关提交给交易柜台"委托(entrust)"
2.交易柜台提交给交易所"订单(order)"

二、关于目前支持的功能

1.下单
只支持限价单(市价单也以限价的形式发送), 条件单、预埋单、FAK&FOK单等暂不支持
2.撤单
只支持限价单的撤单, 条件单、预埋单、FAK&FOK单等暂不支持
3.查询(是否支持查询?)



三、问题清单
0.客户端、服务端目录结构图和设计示意图, 不合理以及需要改进的地方(尤其有关目录设置, 希望有一个参考模板或者规范)
1.客户端需要什么接口
2.有没有CTP的Api详细说明文档或参考资料, 官方文档不够细节, 业务逻辑不清晰
3.CTP连接后出现以下, 但可以运行
/sys/firmware/dmi/tables/smbios_entry_point: Permission denied
/dev/mem: Permission denied
4.CTP连接时产生SIGUSR1, 会中断gdb的调试, SIGUSR1是什么
5.CTP输出的文字在linux命令行下输出为乱码, 将其输出为文件后使用iconv命令可以将其从gbk转换为utf8, 如何更方便的解决
6.希望可以提供一份示例代码, 从示例代码中学习模仿代码规范


CTP等交易柜台提供:
1.每创建一个CTP的Api实例, 登录一个交易帐号